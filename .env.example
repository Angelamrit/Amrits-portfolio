# Copy to .env.local for development. Set the same values in the hosting
# provider's environment for production. Never commit a filled-in copy:
# .gitignore already excludes .env*.

# ---------------------------------------------------------------------------
# Enquiry email (required in production)
# ---------------------------------------------------------------------------
# Without both of these the contact form runs as a dry run: it logs the enquiry
# to the server console and reports success to the guest. That is intended for
# local development only, so the production server refuses to start unless both
# are set — see src/lib/env.ts.

# API key from https://resend.com/api-keys
RESEND_API_KEY=

# Where enquiries are delivered. One address, or several separated by commas.
INQUIRY_TO_EMAIL=chef@example.com

# Optional. The From address on the enquiry and the guest auto-reply. Must be a
# domain verified in Resend. Defaults to Resend's shared onboarding sender,
# which is fine for testing but will land in spam for real guests.
INQUIRY_FROM_EMAIL=Chef Amrit Pal Singh <chef@chefamritpalsingh.com>

# ---------------------------------------------------------------------------
# Site
# ---------------------------------------------------------------------------
# Absolute public URL, used for canonical links, Open Graph tags, the sitemap
# and robots.txt. Defaults to https://chefamritpalsingh.com when unset.
NEXT_PUBLIC_SITE_URL=https://chefamritpalsingh.com

# Set to "true" to show draft journal articles and placeholder content that is
# hidden by default. Leave unset in production.
NEXT_PUBLIC_SHOW_PLACEHOLDERS=

# ---------------------------------------------------------------------------
# Dashboard (/admin)
# ---------------------------------------------------------------------------
# With neither of these set, /admin cannot be signed in to at all: the login
# screen explains what to set and nothing behind it renders.

# Preferred. Generate with:  npm run admin:password
# The password itself is never stored — this is a scrypt hash of it.
ADMIN_PASSWORD_HASH=

# Accepted instead of the hash, for a quick local start. Anything that can read
# this server's environment can sign in with it, so prefer the hash in production.
# Note: a literal $ in any value here is read as a variable reference and has to
# be escaped as \$ — which is why the generated hash above contains none.
ADMIN_PASSWORD=

# Optional. Signs the session cookie. When unset it is derived from the
# credential above, which also means that changing the password signs every
# open session out. Set it explicitly to keep sessions across a password change.
# Generate with:  openssl rand -base64 32
ADMIN_SESSION_SECRET=

# Optional. Where the dashboard writes visitor numbers, content edits and
# uploaded photography. Defaults to .data next to the project.
#
# This must be a directory that survives a restart. On a serverless host
# (Vercel, Netlify) no such directory exists and the data would be lost between
# requests — see the comment at the top of src/lib/store/index.ts.
DATA_DIR=
