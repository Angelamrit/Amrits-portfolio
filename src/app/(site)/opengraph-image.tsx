// The home page sets its own metadata, which replaces the root layout's share
// block, so it needs the card in its own folder too. Same card as the root.
export { alt, size, contentType, default } from "../opengraph-image";
